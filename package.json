{
  "name": "fish-stimulus-desktop",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "tauri:dev": "tauri dev",
    "tauri:build": "python3 backend/build_backend.py && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
